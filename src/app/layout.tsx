import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/lib/theme-context";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SONiC OpenConfig Annotation Framework – End-to-End Guide",
  description:
    "Comprehensive documentation for the SONiC OpenConfig Annotation Framework. Learn about annotation files, transformers, mapping, debugging, and best practices.",
  keywords: [
    "SONiC",
    "OpenConfig",
    "YANG",
    "annotation",
    "transformer",
    "networking",
    "gNMI",
    "Redis",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col">
        <ThemeProvider>{children}</ThemeProvider>
      </body>
    </html>
  );
}
